apiVersion: v1
kind: Service
metadata:
  name: {{ include "hb-app.fullname" . }}
  labels:
    {{- include "hb-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hb-app.selectorLabels" . | nindent 4 }}
